export const saveUserEmail = (email) => ({ type: 'SET_EMAIL', email });

export const saveTotalPrice = (total) => ({ type: 'SET_TOTAL', total });

export const saveExpenses = (expense) => ({ type: 'SET_EXPENSE', expense });
